version = "1.0.0" // Plugin version. Increment this to trigger the updater
description = "Hides your typing status." // Plugin description that will be shown to user

aliucord {
    // Changelog of your plugin
    changelog.set("""
        WOAH. FIRST VERSION?!
        Probably the only one too. This is a one liner, and only used for basic practice.
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}
